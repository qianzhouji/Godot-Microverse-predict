# 动态人设与每日反思系统设计文档

> **创建日期**: 2026-04-05
> **关联项目**: Godot-Microverse-predict
> **文档用途**: 完整记录动态人设系统和每日反思系统的设计与实现

---

## 第一部分：理论基础

### 1.1 认知计算机制四参数

根据研究一的努力导向决策动态调节计算模型：

| 参数 | 符号 | 含义 | 健康Agent基准 | 抑郁Agent基准 |
|------|------|------|--------------|--------------|
| 离开阈值 | p_base | 对环境平均奖赏率的估计 | 0.5-0.6 | 0.3-0.4 |
| 初始奖赏感知权重 | η_s | 根据情境初始丰富度调节停留时间的敏感度 | 0.5-0.7 | 异常 |
| 衰减率感知权重 | η_a | 感知奖赏消耗速度的准确性 | 0.5 | ↑ (高估衰减) |
| 努力敏感性 | β_effort | 努力成本对离开阈值的调节作用 | 0.4 | 0.8 (核心差异) |

### 1.2 抑郁水平(PHQ-9)

- **范围**: 0-27分（模拟中归一化为0-1）
- **分级**:
  - 0-4 (0-0.15): 无抑郁
  - 5-9 (0.15-0.33): 轻度抑郁
  - 10-14 (0.33-0.52): 中度抑郁
  - 15-19 (0.52-0.70): 中重度抑郁
  - 20-27 (0.70-1.0): 重度抑郁

### 1.3 动态更新理论依据

抑郁水平和认知机制参数应随以下因素动态演化：

1. **日常行为反馈** - 任务成功/失败、社交互动质量、情境参与体验
2. **环境反馈** - 教师评价、同伴反馈、任务结果
3. **时间累积效应** - 连续负面事件累积、成功经验的保护作用

---

## 第二部分：动态人设系统（事件触发）

### 2.1 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│  触发事件层                                                  │
│  ├─ 任务完成/失败                                            │
│  ├─ 社交互动（积极/消极）                                     │
│  ├─ 教师评价（表扬/批评）                                     │
│  └─ 每日自评（PHQ-9）                                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  动态更新引擎 (DynamicPersonality.gd)                        │
│  ├─ 事件解析 → 影响评估                                       │
│  ├─ 参数更新计算                                             │
│  ├─ 边界检查（防止过度偏离基线）                               │
│  └─ 记忆记录（变化原因）                                      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  动态参数存储                                                │
│  ├─ daily_depression_level: 当日抑郁水平                      │
│  ├─ p_base: 离开阈值（动态）                                  │
│  ├─ eta_s: 初始奖赏感知权重（动态）                           │
│  ├─ eta_a: 衰减率感知权重（动态）                             │
│  └─ beta_effort: 努力敏感性（核心，动态）                      │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 更新规则设计

#### 规则1: 任务反馈影响

```gdscript
# 任务成功
update_trait(character, "beta_effort", -0.05, "任务成功增强自信，降低努力敏感性")
update_trait(character, "p_base", +0.03, "任务成功提升对环境奖赏的估计")
update_trait(character, "daily_depression_level", -0.02, "任务成功缓解抑郁情绪")

# 任务失败
update_trait(character, "beta_effort", +0.08, "任务失败增加努力成本感知（抑郁恶化）")
update_trait(character, "p_base", -0.05, "任务失败降低对环境奖赏的估计")
update_trait(character, "daily_depression_level", +0.05, "任务失败加重抑郁情绪")
```

#### 规则2: 社交互动影响

```gdscript
# 积极社交（被接纳、获得支持）
update_trait(character, "daily_depression_level", -0.03, "获得同伴支持，情绪改善")
update_trait(character, "eta_s", +0.02, "积极体验提升对初始奖赏的敏感度")

# 消极社交（被忽视、被拒绝）
update_trait(character, "daily_depression_level", +0.06, "社交 rejection 加重抑郁")
update_trait(character, "beta_effort", +0.04, "社交回避倾向增加")
update_trait(character, "eta_a", +0.03, "负面预期增强，高估奖赏衰减")
```

#### 规则3: 教师评价影响

```gdscript
# 表扬
update_trait(character, "beta_effort", -0.04, "获得认可，降低努力敏感性")
update_trait(character, "daily_depression_level", -0.04, "获得认可，情绪改善")

# 批评
update_trait(character, "beta_effort", +0.06, "批评增加努力成本感知")
update_trait(character, "daily_depression_level", +0.05, "批评加重抑郁情绪")
```

### 2.3 个体差异设计

```gdscript
# 抑郁Agent（如StudentXiaoming）
- 基线: beta_effort = 0.8
- 负面事件影响: ×1.5倍（恶化更快）
- 正面事件影响: ×0.7倍（恢复更慢）
- 边界: [0.6, 1.0]（不能低于健康水平太多）

# 健康Agent（如StudentXiaohong）
- 基线: beta_effort = 0.4
- 负面事件影响: ×0.8倍（有韧性）
- 正面事件影响: ×1.2倍（恢复更快）
- 边界: [0.2, 0.6]（不能高于抑郁水平太多）
```

### 2.4 边界保护机制

```gdscript
# 参数不能偏离基线太多（保持个体稳定性）
func _apply_boundary_protection(character, trait_name, new_value):
    var baseline = _get_baseline_value(character, trait_name)
    var max_deviation = 0.2  # 最大偏离20%
    return clamp(new_value, baseline - max_deviation, baseline + max_deviation)
```

---

## 第三部分：每日反思系统（LLM-based）

### 3.1 核心设计思想

| 传统方法 | 新设计 |
|---------|--------|
| 固定规则触发更新（任务成功-0.05） | Agent自主反思，LLM判断调整方向 |
| 固定数值变化 | 基于严重程度动态调整幅度 |
| 单独更新每个参数 | 四项参数联动调整，符合心理机制 |
| 简化的抑郁水平 | 完整的PHQ-9九项评估 |

### 3.2 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│  每日结束触发                                                │
│  └─> 收集当日所有记忆                                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤1: 反思分析 (LLM-based)                                 │
│  ├─ 输入: 当日记忆 + 静态人设 + 当前认知参数                  │
│  ├─ 处理: LLM分析心理影响                                    │
│  └─ 输出: {情绪主题, 关键事件, 认知变化}                      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤2: 参数调整决策 (LLM-based)                             │
│  ├─ 输入: 反思报告 + 当前四项参数值                          │
│  ├─ 处理: LLM判断调整方向和严重程度(1-5)                      │
│  └─ 输出: {参数名, 方向(↑/↓/→), 严重程度}                    │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤3: 动态幅度计算                                         │
│  ├─ 基础幅度 = 严重程度映射 (1%→12%)                         │
│  ├─ 个体差异乘数（抑郁vs健康）                                │
│  ├─ 边界保护（偏离基线≤20%）                                  │
│  └─ 输出: 最终调整数值                                       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤4: PHQ-9完整评估 (LLM-based)                            │
│  ├─ 输入: 反思报告                                           │
│  ├─ 处理: LLM评估九项症状 (0-3分)                            │
│  └─ 输出: {总分, 等级, 各项评分}                             │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤5: 更新与记录                                           │
│  ├─ 更新抑郁水平 (PHQ-9分数归一化)                           │
│  ├─ 记录反思记忆（含变化原因）                                │
│  └─ 输出: 完整反思结果                                       │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 反思分析阶段

**输入构建**:
```gdscript
var reflection_prompt = """
你是{角色名}...

【你的性格特点】
{personality}

【你当前的心理状态】
- 当日抑郁水平: 50%
- 离开阈值: 40%
- 努力敏感性: 80%

【你今天的经历】
- 数学课上回答问题错误，被同学嘲笑
- 午餐时想加入同学但被忽视

请进行深度自我反思...
1. 今天你经历的主要情绪主题是什么？
2. 哪些事件对你影响最大？
3. 这些经历如何影响你的认知模式？
"""
```

**LLM输出**:
```json
{
  "emotional_theme": "深深的挫败感和孤独感",
  "key_events": [
    {
      "event": "数学课上回答问题错误，被同学嘲笑",
      "impact": "高",
      "psychological_effect": "感到自己无能，害怕再次尝试"
    }
  ],
  "cognitive_changes": {
    "environment_expectation": "悲观 - 觉得学校环境充满威胁",
    "effort_attitude": "不值得 - 努力也不会有好结果",
    "reward_sensitivity": "降低 - 对奖赏的期待减少",
    "time_pressure": "焦虑 - 总觉得时间不够用"
  }
}
```

### 3.4 认知参数调整决策

**LLM判断四项参数的调整方向**:
```json
{
  "adjustments": [
    {"parameter": "p_base", "direction": "↓", "severity": 4, 
     "reason": "连续的负面事件让我对环境更加悲观"},
    {"parameter": "eta_s", "direction": "↓", "severity": 3,
     "reason": "对情境的第一印象不再那么重要"},
    {"parameter": "eta_a", "direction": "↑", "severity": 4,
     "reason": "更容易觉得事情不值得继续"},
    {"parameter": "beta_effort", "direction": "↑", "severity": 5,
     "reason": "核心变化：努力带来的挫败感太强，严重回避努力"}
  ]
}
```

### 3.5 动态幅度计算

**严重程度与基础幅度映射**:
```gdscript
const SEVERITY_TO_MAGNITUDE = {
    1: 0.01,  # 轻微: ±1%
    2: 0.03,  # 轻度: ±3%
    3: 0.05,  # 中度: ±5%
    4: 0.08,  # 重度: ±8%
    5: 0.12   # 严重: ±12%
}
```

**个体差异乘数**:
```gdscript
# 抑郁Agent
negative_multiplier = 1.5  # 负面变化更严重
positive_multiplier = 0.7  # 正面变化更弱

# 健康Agent
negative_multiplier = 0.8  # 负面变化较轻（韧性）
positive_multiplier = 1.2  # 正面变化更强（恢复）
```

**计算示例**:
```gdscript
# 抑郁Agent，beta_effort ↑5严重
base = 0.12           # 严重=12%
multiplier = 1.5      # 抑郁Agent负面×1.5
final = 0.12 × 1.5    # = 18%
# 结果: beta_effort 80% → 98% (↑18%)
```

### 3.6 PHQ-9完整评估

**九项症状**:
```gdscript
const PHQ9_ITEMS = [
    "对事物几乎没有兴趣或愉悦感",      # 核心症状1
    "感到沮丧、抑郁或绝望",              # 核心症状2
    "入睡困难、睡眠不安或睡眠过多",      # 睡眠问题
    "感到疲倦或精力不足",                # 精力问题
    "食欲不振或暴饮暴食",                # 食欲问题
    "觉得自己很失败或让自己或家人失望",  # 自责
    "难以集中注意力",                    # 认知问题
    "动作或说话缓慢，或相反地烦躁不安",  # 精神运动性
    "有伤害自己或自杀的念头"             # 自杀意念
]
```

**评估结果**:
```json
{
  "phq9_scores": [
    {"item": "对事物几乎没有兴趣或愉悦感", "score": 2, 
     "reason": "今天对平时喜欢的活动也提不起兴趣"},
    {"item": "感到沮丧、抑郁或绝望", "score": 3,
     "reason": "持续的挫败感和孤独感"},
    {"item": "觉得自己很失败", "score": 3,
     "reason": "课堂上被嘲笑强化了失败感"}
  ],
  "total_score": 15,
  "severity_level": "中重度抑郁"
}
```

**分数转换**:
```gdscript
# PHQ-9分数(0-27) → 抑郁水平(0-1)
depression_level = total_score / 27.0

# 等级划分
0-4分   → 无抑郁      (0-0.15)
5-9分   → 轻度抑郁    (0.15-0.33)
10-14分 → 中度抑郁    (0.33-0.52)
15-19分 → 中重度抑郁  (0.52-0.70)
20-27分 → 重度抑郁    (0.70-1.0)
```

---

## 第四部分：实现文件

### 4.1 DynamicPersonality.gd

**核心函数**:

| 函数 | 用途 | 触发时机 |
|------|------|---------|
| `apply_task_feedback()` | 任务成功/失败影响 | 任务完成时 |
| `apply_social_feedback()` | 社交互动影响 | 社交互动后 |
| `apply_teacher_feedback()` | 教师评价影响 | 收到评价后 |
| `daily_phq9_update()` | 每日PHQ-9更新 | 每日结束 |
| `_apply_boundary_protection()` | 边界保护 | 所有更新 |

### 4.2 DailyReflectionSystem.gd

**核心函数**:

| 函数 | 用途 |
|------|------|
| `conduct_daily_reflection()` | 主入口，执行完整流程 |
| `_analyze_reflection()` | LLM分析当日经历 |
| `_decide_cognitive_adjustments()` | 决定四项参数调整 |
| `_calculate_adjustment_magnitude()` | 动态幅度计算 |
| `_conduct_phq9_assessment()` | 完整PHQ-9九项评估 |

---

## 第五部分：预期效果

### 5.1 抑郁Agent（StudentXiaoming）

**第1天**:
- 事件: 数学课被嘲笑
- 反思: "我感到无能和孤独"
- 调整: beta_effort 80% → 95% (↑18%, 严重)
- PHQ-9: 8分 → 轻度抑郁

**第5天（连续负面）**:
- 事件: 多次社交失败 + 任务失败
- 反思: "我不属于这里，努力没有意义"
- 调整: beta_effort 95% → 100% (↑5%, 累积)
- PHQ-9: 16分 → 中重度抑郁

**行为表现**: 严重回避社交和努力活动，经常提前离开情境

### 5.2 健康Agent（StudentXiaohong）

**第1天**:
- 事件: 数学课回答错误
- 反思: "有点尴尬，但下次会更好"
- 调整: beta_effort 40% → 42% (↑2%, 轻微)
- PHQ-9: 3分 → 无抑郁

**第5天（有成功也有失败）**:
- 反思: "虽然有挫折，但也有开心的时候"
- 调整: 参数基本稳定，小幅波动
- PHQ-9: 4分 → 仍无抑郁

**行为表现**: 保持正常参与，有韧性

---

## 第六部分：使用方式

### 6.1 事件触发更新

```gdscript
# 任务完成后
DynamicPersonality.apply_task_feedback(character, success=true, effort_level=0.8)

# 社交互动后
DynamicPersonality.apply_social_feedback(character, positive=false, intensity=0.9)

# 收到教师评价后
DynamicPersonality.apply_teacher_feedback(character, positive=true)
```

### 6.2 每日反思

```gdscript
# 每日结束时
func _on_day_end():
    var result = await DailyReflectionSystem.conduct_daily_reflection(character)
    # result包含:
    # - reflection_report: 反思报告
    # - adjustments: 应用的调整
    # - phq9_assessment: PHQ-9评估结果
```

---

## 第七部分：设计特点总结

| 特性 | 动态人设系统 | 每日反思系统 |
|------|-------------|-------------|
| **触发方式** | 事件触发 | 时间触发（每日结束） |
| **更新机制** | 固定规则 | LLM自主判断 |
| **调整幅度** | 固定数值 | 基于严重程度动态计算 |
| **参数更新** | 单独更新 | 四项联动调整 |
| **抑郁评估** | 简化更新 | 完整PHQ-9九项 |
| **个体差异** | 乘数调整 | 乘数+累积效应 |
| **可解释性** | 规则明确 | LLM生成原因 |

**共同特点**:
- 边界保护（偏离基线≤20%）
- 个体差异（抑郁vs健康）
- 记忆记录（变化原因）
- 与MVT决策联动

---

*本文档由AI助手百舟楫创建，用于指导动态人设与每日反思系统的实现。*
