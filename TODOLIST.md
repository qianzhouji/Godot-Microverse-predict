# 抑郁风险学生模拟系统 - 重构任务清单（V2.0）

> **创建日期**: 2026-04-06
> **项目**: Godot-Microverse-predict
> **状态**: 架构重构中

---

## 核心架构：四层系统 + 中央时序控制

```
┌─────────────────────────────────────────────────────────────┐
│                    第一层：中央时序系统                        │
│  - 全局时钟（5分钟/周期）                                      │
│  - 上升沿触发所有Agent活动                                     │
│  - 请求缓存与批准执行                                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    第二层：全局状态系统                        │
│  - 时间轴状态（课程表：上课/午休/放学）                         │
│  - 场景状态（各子场景的当前活动）                               │
│  - 活动管理系统（奖赏计算与发放）                               │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    第三层：Agent认知系统                       │
│  - 感知（场景信息获取）                                        │
│  - 体验（主观体验奖赏）                                        │
│  - 决策（行动请求生成）                                        │
│  - 行动请求缓存（最多2步）                                     │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    第四层：对话管理系统                        │
│  - 中范围对话（小组讨论）                                      │
│  - 大范围对话（教师提问）                                      │
│  - 发言优先级队列                                              │
│  - 对话生命周期管理                                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 一、中央时序系统（核心控制器）⏳

### 1.1 功能定位
- **唯一时钟源**：所有Agent活动的同步基准
- **上升沿触发**：游戏时间每5分钟触发一次全局更新
- **请求调度器**：收集、缓存、批准所有Agent的行动请求

### 1.2 时序周期（5分钟游戏时间）

```
Click N (上升沿触发)
    │
    ├── 批准Click N-1缓存的所有请求
    │   ├── Agent A: 开始移动
    │   ├── Agent B: 结束对话
    │   └── Agent C: 开始对话
    │
    ├── 执行所有批准的行动
    │   └── （移动、开始/结束对话等）
    │
    ├── 行动完成后 → 触发体验+感知
    │   ├── 体验：基于场景参数计算客观奖赏
    │   └── 感知：获取当前场景信息
    │
    ├── 感知完成后 → 自动触发决策
    │   └── 生成行动请求 → 缓存到下一次Click
    │
    └── 等待下一次Click...

Click N+1 (5分钟后)
    └── 重复上述流程
```

### 1.3 请求类型与处理

| 请求类型 | 触发时机 | 批准执行 | 说明 |
|---------|---------|---------|------|
| **开始行动** | 决策后 | Click时刻 | 移动、开始对话、开始自习等 |
| **结束行动** | 决策后/超时 | Click时刻 | 结束对话、结束自习等 |
| **对话发言** | 对话进行中 | 即时 | 不受Click限制，按优先级队列 |

### 1.4 决策超时处理

```
决策阶段（感知后自动开始）
    │
    ├── 在5分钟内完成决策
    │   └── 提交行动请求 → 缓存等待Click
    │
    └── 超过5分钟未完成
        └── 跳过本次Click
            └── 等待下一次Click重新决策
```

### 1.5 每日流程

```
08:00 游戏日开始
    └── 所有Agent统一感知+决策

08:00-17:00 正常运行
    └── 每5分钟一个Click周期

17:00 放学时间
    └── 不接受新的开始请求
    └── 只允许结束当前活动

17:30 强制结束
    └── 所有Agent进入反思阶段
    └── 更新记忆、情感关系
    └── 完成后通知时序系统

次日08:00 新游戏日开始
```

---

## 二、全局状态系统（上层约束）⏳

### 2.1 时间轴状态（课程表）

```gdscript
class TimelineState:
    var current_time: float           # 当前游戏时间
    var current_period: String        # 当前时段
    # "class_time" / "break_time" / "lunch_time" / "after_school"
    
    func get_current_constraints() -> Dictionary:
        # 返回当前时段的行为约束
        match current_period:
            "class_time":
                return {
                    "can_speak_freely": false,    # 不能随意讲话
                    "can_leave_room": false,       # 不能随便离开
                    "must_follow_teacher": true    # 必须跟随教师指令
                }
            "break_time":
                return {
                    "can_speak_freely": true,
                    "can_leave_room": true,
                    "must_follow_teacher": false
                }
```

### 2.2 活动管理系统

**活动类型**：

| 活动类型 | 触发条件 | 场景限制 | 结束条件 |
|---------|---------|---------|---------|
| **上课听讲** | 时间轴+在教室 | 教室 | 下课Click |
| **小组讨论** | 决策发起 | 任意（中范围） | 15分钟/无人回复 |
| **教师提问** | 教师发起 | 教室（大范围） | 回答完成 |
| **体育活动** | 决策发起 | 体育馆 | 决策结束 |
| **自习** | 决策发起 | 图书馆/自习室 | 决策结束 |
| **移动** | 决策发起 | 任意 | 到达目标 |

**奖赏计算**（系统层 → Agent）：

```gdscript
func calculate_reward(activity: String, room_params: Dictionary, 
                     agent_params: Dictionary) -> float:
    # 基于场景客观参数计算奖赏
    var base_reward = room_params.initial_reward_rate
    var decay = room_params.reward_decay_rate
    var effort = room_params.effort_level
    var time_spent = agent_params.time_in_activity
    
    # 客观奖赏公式（系统层计算）
    var objective_reward = base_reward * pow(1 - decay, time_spent) - effort
    
    return objective_reward
```

---

## 三、Agent认知系统（核心循环）⏳

### 3.1 认知循环：感知 → 体验 → 决策

```
Click触发
    │
    ▼
┌─────────────┐
│   感知阶段   │  ← 获取当前场景信息
│  (自动执行)  │
└─────────────┘
    │
    ├── 当前子场景
    ├── 子场景内其他Agent
    ├── 可见的对话行为
    ├── 可听到的对话内容
    └── 时间轴状态（上课/休息）
    │
    ▼
┌─────────────┐
│   体验阶段   │  ← 主观体验系统层奖赏
│  (自动执行)  │
└─────────────┘
    │
    ├── 接收系统层奖赏（客观值）
    ├── 贝叶斯更新主观感知
    └── 更新认知参数（p_base, η_s, η_a, β_effort）
    │
    ▼
┌─────────────┐
│   决策阶段   │  ← 生成行动请求
│  (自动执行)  │
└─────────────┘
    │
    ├── 结合感知信息
    ├── 结合认知参数
    ├── 结合时间轴约束
    └── 生成行动请求（最多缓存2步）
    │
    ▼
等待下一次Click执行
```

### 3.2 决策逻辑与缓存

**两步缓存机制**：

```gdscript
class AgentDecisionCache:
    var step1_request: ActionRequest    # 第一步请求
    var step2_request: ActionRequest    # 第二步请求（可选）
    
    func execute_step1():
        # Click时刻执行第一步
        if step1_request:
            execute_action(step1_request)
            
            # 执行后需要重新感知+决策
            if step2_request:
                # 检查step2是否仍然有效
                var current_perception = perceive()
                if is_request_still_valid(step2_request, current_perception):
                    # 有效，继续执行step2
                    return step2_request
                else:
                    # 无效（如目标已离开），重新决策
                    return make_new_decision()
```

**示例场景**：

```
Agent A想和Agent B对话，但不在同一中范围

决策结果：
├─ Step1: 移动路径（进入B所在中范围）
└─ Step2: 开始对话（缓存，待确认）

Click N: 执行Step1（移动）
    │
    └── 到达后 → 立即感知（无体验）
        │
        └── 检查：
            ├─ B是否还在？ → 是
            ├─ B是否已在对话？ → 否
            └─ 是否上课了？ → 否
        │
        └── 全部通过 → 执行Step2（开始对话）
        
        任一检查失败 → 重新决策
```

### 3.3 可执行的行动

| 行动 | 场景限制 | 触发条件 | 说明 |
|------|---------|---------|------|
| **路径移动** | 任意 | 决策生成 | 移动到目标中范围 |
| **开始对话** | 任意 | 决策生成 | 发起新话题 |
| **加入对话** | 任意 | 决策生成 | 加入已有对话 |
| **退出对话** | 对话中 | 决策生成/超时 | 主动退出或15分钟超时 |
| **开始体育活动** | 体育馆 | 决策生成 | 开始锻炼 |
| **结束体育活动** | 体育馆 | 决策生成 | 结束锻炼 |
| **开始自习** | 图书馆/自习室 | 决策生成 | 开始自习 |
| **结束自习** | 图书馆/自习室 | 决策生成 | 结束自习 |

---

## 四、对话管理系统（并行子系统）⏳

### 4.1 系统定位
- **独立于Click周期**：对话发言不等待Click
- **优先级队列管理**：多人同时想发言时的排序
- **生命周期管理**：对话开始、进行、结束

### 4.2 中范围对话（小组讨论）

```
对话生命周期
    │
    ├── 开始（需Click触发）
    │   └── 某人发起话题 → 等待Click → 对话开始
    │
    ├── 进行（不受Click限制）
    │   └── 发言者A结束
    │       └── 同中范围所有Agent思考：是否回复？
    │           └── 将决定（回复/不回复）提交给对话管理系统
    │               └── 系统按优先级排序决定回复者
    │                   └── 优先级最高者发言
    │                       └── 循环...
    │
    └── 结束
        ├── 条件1：某轮次无人选择回复（全员沉默）
        ├── 条件2：对话持续15分钟
        └── 结束动作：所有Agent提交"退出对话"请求
            └── 等待Click后退出+体验+感知
```

### 4.3 大范围对话（教师提问）

```
教师发起提问（大范围）
    │
    ├── 教师Agent决定是否需要回复
    │   └── 通知类：无需回复 → 等待Click → 下一轮
    │   └── 提问类：需要回复 → 继续
    │
    ├── 子场景内所有Agent思考：是否回答？
    │   └── 将决定提交给系统
    │
    ├── 系统汇总所有"想回答"的Agent
    │   └── 报告给教师："A、B、C想回答"
    │
    ├── 教师Agent思考：选择谁回答？
    │   └── 回复系统："选择A"
    │
    ├── 系统指定A回答
    │   └── A发言
    │
    └── 回答完成 → 对话结束 → 等待Click
```

### 4.4 发言优先级系统

**优先级计算**：

```gdscript
func calculate_speak_priority(agent: Agent, context: DialogueContext) -> float:
    var priority = 0.0
    
    # 1. 身份权重
    if agent.is_teacher:
        priority += 100.0
    
    # 2. 连续发言惩罚
    if agent == context.last_speaker:
        priority -= 20.0
    
    # 3. 外向性格加成
    priority += agent.personality.extraversion * 10.0
    
    # 4. 情感关系加成（与当前话题相关者）
    if context.topic_initiator:
        var relationship = agent.get_relationship(context.topic_initiator)
        priority += relationship.intensity * 5.0
    
    # 5. 话题相关性（与当前讨论内容的相关度）
    priority += context.topic_relevance * 15.0
    
    # 6. 等待时间（在队列中等待越久优先级越高）
    priority += context.waiting_time * 2.0
    
    return priority
```

**打断规则**：

| 打断条件 | 说明 |
|---------|------|
| 教师特权 | 教师可随时打断学生 |
| 显著优先 | 优先级高出30以上可打断 |
| 超时打断 | 当前发言超过30秒可打断 |

---

## 五、系统间调用关系

### 5.1 调用顺序图

```
中央时序系统
    │
    ├── 调用 → 全局状态系统（获取时间轴约束）
    │
    ├── 调用 → Agent认知系统（触发感知+体验+决策）
    │       │
    │       ├── 调用 → 系统层（获取场景参数）
    │       │
    │       ├── 调用 → 感知层（贝叶斯更新）
    │       │
    │       └── 生成 → 行动请求（缓存）
    │
    ├── 调用 → 活动管理系统（计算奖赏）
    │
    └── 调用 → 对话管理系统（对话生命周期）
            │
            └── 调用 → Agent认知系统（思考是否回复）
```

### 5.2 数据流向

```
系统层 → 感知层 → 认知层 → 决策层 → 行动请求 → 系统层
    ↑                                                  │
    └────────────── 体验反馈 ← 行动执行 ←─────────────┘
```

---

## 六、待实现任务清单

### P0 - 核心基础设施

- [ ] **中央时序系统**
  - [ ] 全局时钟（5分钟/Click）
  - [ ] 上升沿触发机制
  - [ ] 请求缓存与批准执行
  
- [ ] **全局状态系统**
  - [ ] 时间轴状态（课程表）
  - [ ] 场景状态管理
  - [ ] 活动管理系统（奖赏计算）

- [ ] **Agent认知系统重构**
  - [ ] 感知-体验-决策循环
  - [ ] 两步缓存机制
  - [ ] 决策超时处理

### P1 - 对话系统

- [ ] **对话管理系统**
  - [ ] 中范围对话（小组讨论）
  - [ ] 大范围对话（教师提问）
  - [ ] 发言优先级队列
  - [ ] 对话生命周期管理

- [ ] **行为与内容分离**
  - [ ] 行为层（全子场景可见）
  - [ ] 内容层（仅范围内可见）
  - [ ] 好奇心机制

### P2 - 情感与记忆

- [ ] **情感关系系统**
  - [ ] 情感类型定义
  - [ ] 每日反思集成
  - [ ] 情感影响决策

- [ ] **记忆系统优化**
  - [ ] 对话记忆存储
  - [ ] 活动记忆存储
  - [ ] 记忆检索优化

### P3 - 优化与完善

- [ ] **可视化调试**
  - [ ] 时序系统状态面板
  - [ ] 对话状态监控
  - [ ] Agent状态实时显示

- [ ] **性能优化**
  - [ ] API调用批处理
  - [ ] 决策缓存机制
  - [ ] 并发处理优化

---

## 七、文件变更计划

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `script/system/TimingSystem.gd` | 新增 | 中央时序系统 |
| `script/system/TimelineState.gd` | 新增 | 时间轴状态管理 |
| `script/system/ActivityManager.gd` | 新增 | 活动管理与奖赏计算 |
| `script/ai/AIAgent.gd` | 重写 | 感知-体验-决策循环 |
| `script/ai/DialogueManager.gd` | 新增 | 对话管理系统 |
| `script/ai/DialoguePerception.gd` | 新增 | 对话感知状态 |
| `script/ai/PriorityQueue.gd` | 新增 | 发言优先级队列 |
| `script/system/TaskManager.gd` | 删除 | 原任务系统废弃 |
| `script/ai/ConversationManager.gd` | 删除 | 原对话系统废弃 |
| `script/ai/DialogManager.gd` | 删除 | 原对话系统废弃 |
| `script/ai/DialogService.gd` | 删除 | 原对话系统废弃 |

---

*最后更新：2026-04-06*
*维护者：百舟楫*
